{{- if and .Values.cartService.create .Values.cartService.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.cartService.name }}-hpa
  namespace: loadtesting
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.cartService.name }}
  minReplicas: {{ .Values.cartService.hpa.minReplicas | default 1 }}
  maxReplicas: {{ .Values.cartService.hpa.maxReplicas | default 5 }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.cartService.hpa.targetCPUUtilizationPercentage | default 80 }}
{{- end }}