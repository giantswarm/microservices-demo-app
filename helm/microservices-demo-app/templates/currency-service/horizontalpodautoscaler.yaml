{{- if and .Values.currencyService.create .Values.currencyService.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.currencyService.name }}-hpa
  namespace: loadtesting
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.currencyService.name }}
  minReplicas: {{ .Values.currencyService.hpa.minReplicas | default 1 }}
  maxReplicas: {{ .Values.currencyService.hpa.maxReplicas | default 5 }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.currencyService.hpa.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
