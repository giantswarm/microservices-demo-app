{{- if and .Values.shippingService.create .Values.shippingService.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.shippingService.name }}-hpa
  namespace: loadtesting
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.shippingService.name }}
  minReplicas: {{ .Values.shippingService.hpa.minReplicas | default 1 }}
  maxReplicas: {{ .Values.shippingService.hpa.maxReplicas | default 5 }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.shippingService.hpa.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
