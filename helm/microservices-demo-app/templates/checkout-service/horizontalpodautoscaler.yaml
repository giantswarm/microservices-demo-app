{{- if and .Values.checkoutService.create .Values.checkoutService.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.checkoutService.name }}-hpa
  namespace: loadtesting
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.checkoutService.name }}
  minReplicas: {{ .Values.checkoutService.hpa.minReplicas | default 1 }}
  maxReplicas: {{ .Values.checkoutService.hpa.maxReplicas | default 5 }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.checkoutService.hpa.targetCPUUtilizationPercentage | default 80 }}
{{- end }}