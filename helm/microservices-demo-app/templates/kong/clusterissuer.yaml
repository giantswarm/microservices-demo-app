{{- if and .Values.kong.enabled .Values.kong.customIssuer.enabled }}
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-giantswarm-nginx
spec:
  acme:
    email: accounts@giantswarm.io
    privateKeySecretRef:
      name: letsencrypt-giantswarm-nginx
    server: https://acme-v02.api.letsencrypt.org/directory
    solvers:
    - dns01:
        acmeDNS:
          host: {{ .Values.kong.customIssuer.acmeDnsHost }}
          accountSecretRef:
            name: {{ .Values.kong.customIssuer.acmeDnsSecretName }}
            key: {{ .Values.kong.customIssuer.acmeDnsSecretKey }}
{{- end }}
