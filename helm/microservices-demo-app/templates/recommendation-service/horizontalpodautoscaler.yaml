{{- if and .Values.recommendationService.create .Values.recommendationService.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.recommendationService.name }}-hpa
  namespace: loadtesting
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.recommendationService.name }}
  minReplicas: {{ .Values.recommendationService.hpa.minReplicas | default 1 }}
  maxReplicas: {{ .Values.recommendationService.hpa.maxReplicas | default 5 }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.recommendationService.hpa.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
