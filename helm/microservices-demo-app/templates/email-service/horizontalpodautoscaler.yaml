{{- if and .Values.emailService.create .Values.emailService.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.emailService.name }}-hpa
  namespace: loadtesting
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.emailService.name }}
  minReplicas: {{ .Values.emailService.hpa.minReplicas | default 1 }}
  maxReplicas: {{ .Values.emailService.hpa.maxReplicas | default 5 }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.emailService.hpa.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
